import "./Hero.scss"

export const Hero = () => {
    return (
        <section className="hero container">
            <h1 className="hero-title">Your desired dish?</h1>
            <p className="hero-text">Search any recipe e.g: burger, pizza, sandwich, toast.</p>
        </section>
    )
}
